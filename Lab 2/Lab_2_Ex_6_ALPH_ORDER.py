# This code sorts the keys of a dictionary and prints the sorted keys

# Create a dictionary with 4 key-value pairs
dictionary = {'b': 3, 'c': 2, 'a': 4, 'd': 1}

# Convert the keys of the dictionary to a list
keys = list(dictionary.keys())

# Sort the keys of the list in ascending order
keys.sort()

# Print the sorted keys
print(keys)